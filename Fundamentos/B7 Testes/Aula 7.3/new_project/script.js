const sub = (a, b) => {
  return a - b;
};

const saudacao = (nome) => {
  return `Maaa oeeeeeee ${nome}`;
};

const pets = ["Loki", "Kiara", "Arthur", "Pagod", "Pipoca", "Arley"];
// exportando
module.exports = { sub, saudacao, pets };
